{
  "id": "socialkitCompetitorMonitorV1",
  "name": "SocialKit Competitor Monitor v0.1.0",
  "nodes": [
    {
      "id": "manual-run",
      "name": "Manual run",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        0,
        0
      ],
      "parameters": {}
    },
    {
      "id": "daily-schedule",
      "name": "Daily schedule",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1,
      "position": [
        0,
        200
      ],
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "days",
              "daysInterval": 1,
              "triggerAtHour": 9
            }
          ]
        }
      }
    },
    {
      "id": "configure-competitors",
      "name": "Configure competitors",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        240,
        0
      ],
      "parameters": {
        "jsCode": "/** Pure normalization shared by the CLI and generated n8n artifact. */\nconst VERSION = '1.0.0';\n// Deliberately small HTTP parser: n8n Code sandboxes do not expose the URL global.\n// Reject authority credentials, ports, whitespace and backslashes; caller validates exact paths.\nfunction publicUrl(value) {\n  const m = typeof value === 'string' && /^(https?):\\/\\/([A-Za-z0-9.-]+)(\\/[^?#\\s\\\\]*)?(\\?[^#\\s\\\\]*)?(#[^\\s\\\\]*)?$/i.exec(value);\n  if (!m) throw new Error('Invalid public URL.');\n  const params = (m[4] || '').slice(1).split('&').filter(Boolean).map(p => p.split('='));\n  return { protocol: m[1].toLowerCase() + ':', hostname: m[2].toLowerCase(), pathname: m[3] || '/', search: m[4] || '', hash: m[5] || '', searchParams: { get: key => {const matches=params.filter(p=>p[0]===key);return matches.length===1?matches[0][1]:null;} } };\n}\nfunction parseProfile(value) {\n  let u;\n  try { u = publicUrl(value); } catch { throw new Error('Use a full public Instagram profile or YouTube channel URL.'); }\n  if (!['https:', 'http:'].includes(u.protocol) || u.username || u.password || u.port || u.search || u.hash) throw new Error('Profile URLs must not contain credentials, ports, queries, or fragments.');\n  const path = u.pathname.replace(/\\/+$/, '');\n  if (['youtube.com', 'www.youtube.com', 'm.youtube.com'].includes(u.hostname) && /^\\/(?:@[\\p{L}\\p{N}_.-]+|(?:channel|c|user)\\/[A-Za-z0-9_.-]+)$/u.test(path)) {\n    return { platform: 'youtube', url: `https://www.youtube.com${path}`, endpoint: '/youtube/videos' };\n  }\n  if (['instagram.com', 'www.instagram.com', 'm.instagram.com'].includes(u.hostname) && /^\\/[A-Za-z0-9_.]{1,30}$/.test(path) && !['p', 'reel', 'reels', 'tv', 'explore', 'accounts', 'stories'].includes(path.slice(1).toLowerCase())) {\n    return { platform: 'instagram', url: `https://www.instagram.com${path.toLowerCase()}/`, endpoint: '/instagram/channel-reels' };\n  }\n  throw new Error('Expected a YouTube channel URL or Instagram profile URL, not a post, video, or playlist.');\n}\nfunction normalizeProfiles(values) {\n  if (!Array.isArray(values) || values.length < 1 || values.length > 3) throw new Error('Configure one to three profiles.');\n  const profiles = values.map(parseProfile);\n  if (new Set(profiles.map(p => p.url)).size !== profiles.length) throw new Error('Profiles must be distinct.');\n  return profiles;\n}\nfunction listCredits(platform, limit) { return Math.max(1, Math.ceil(limit / (platform === 'instagram' ? 20 : 50))); }\nfunction metric(value) { return typeof value === 'number' && Number.isFinite(value) && value >= 0 ? value : null; }\nfunction instant(value) {\n  if (typeof value !== 'string' || !/^\\d{4}-\\d{2}-\\d{2}T/.test(value)) return null;\n  const t = Date.parse(value); return Number.isFinite(t) ? new Date(t).toISOString() : null;\n}\nfunction textValue(value) { return typeof value === 'string' && value.trim() ? value.trim() : null; }\nfunction normalizeItem(item, profile, fetchedAt) {\n  if (!item || typeof item !== 'object') throw new Error('Invalid item object.');\n  let u; try { u = publicUrl(item.url); } catch { throw new Error('Missing or invalid source URL.'); }\n  if (!['http:', 'https:'].includes(u.protocol) || u.username || u.password || u.port) throw new Error('Unsafe source URL.');\n  let id, sourceUrl, contentType;\n  if (profile.platform === 'youtube') {\n    if (!['youtube.com', 'www.youtube.com', 'm.youtube.com', 'youtu.be'].includes(u.hostname)) throw new Error('Wrong source host.');\n    const urlId = u.hostname === 'youtu.be' ? u.pathname.slice(1) : u.pathname === '/watch' ? u.searchParams.get('v') : /^\\/shorts\\/([\\w-]{11})\\/?$/.exec(u.pathname)?.[1];\n    id = item.videoId;\n    if (!/^[\\w-]{11}$/.test(id || '') || id !== urlId) throw new Error('Video ID does not match source URL.');\n    contentType = u.pathname.startsWith('/shorts/') || item.contentType === 'short' ? 'short' : 'video';\n    sourceUrl = `https://www.youtube.com/watch?v=${id}`;\n  } else {\n    if (!['instagram.com', 'www.instagram.com', 'm.instagram.com'].includes(u.hostname)) throw new Error('Wrong source host.');\n    const urlId = /^\\/(?:p|reel|reels)\\/([\\w-]+)\\/?$/.exec(u.pathname)?.[1];\n    id = item.shortcode;\n    if (!/^[\\w-]{1,64}$/.test(id || '') || id !== urlId) throw new Error('Reel shortcode does not match source URL.');\n    if (item.type !== 'reel' && item.productType !== 'clips') throw new Error('Non-Reel item in Reels feed.');\n    const author = item.author?.username;\n    contentType = 'reel'; sourceUrl = `https://www.instagram.com/reel/${id}/`;\n  }\n  let publishedAt = instant(item.publishedAt);\n  if (profile.platform === 'instagram' && typeof item.timestamp === 'number' && item.timestamp > 0 && item.timestamp < 8640000000000) publishedAt = new Date(item.timestamp * 1000).toISOString();\n  return { id: `${profile.platform}:${id}`, platform: profile.platform, contentType, profileUrl: profile.url, sourceUrl,\n    authorUsername: profile.platform === 'instagram' ? textValue(item.author?.username) : null,\n    title: textValue(item.title), description: textValue(profile.platform === 'instagram' ? item.caption : item.description),\n    publishedAt, fetchedAt, metrics: { views: metric(item.views), likes: metric(item.likes), comments: metric(item.comments) } };\n}\nfunction normalizeResponse(body, profile, limit, fetchedAt) {\n  if (body?.success !== true || !body.data) throw new Error('API did not return a successful data object.');\n  const data = body.data;\n  const identity = profile.platform === 'instagram' ? data.profileUrl : data.url;\n  if (!identity && !(profile.platform === 'instagram' && typeof data.username === 'string' && data.username)) throw new Error('Response is missing a verifiable profile identity.');\n  if (identity && parseProfile(identity).url !== profile.url) throw new Error('Response profile identity does not match request.');\n  if (profile.platform === 'instagram' && data.username && data.username.toLowerCase() !== publicUrl(profile.url).pathname.split('/')[1]) throw new Error('Response username does not match request.');\n  const source = profile.platform === 'youtube' ? data.results : data.items;\n  if (!Array.isArray(source)) throw new Error('Response is missing the expected item array.');\n  if (source.length > limit) throw new Error('API exceeded requested item limit; stop further requests.');\n  const warnings = [], items = [], seen = new Set();\n  for (const [index, raw] of source.entries()) {\n    try { const item = normalizeItem(raw, profile, fetchedAt); if (!seen.has(item.id)) { items.push(item); seen.add(item.id); } else warnings.push(`Duplicate item ${index + 1} omitted.`); }\n    catch (error) { warnings.push(`Item ${index + 1} rejected: ${error.message}`); }\n  }\n  if (source.length && !items.length) throw new Error(`All ${source.length} items failed identity/schema validation.`);\n  for (const item of items) if (item.authorUsername && item.authorUsername.toLowerCase() !== publicUrl(profile.url).pathname.split('/')[1]) warnings.push(`${item.id} has primary author @${item.authorUsername}; feed membership does not prove original authorship (shared/collaborative posts may differ).`);\n  if (data.hasMore) warnings.push('More items exist; continuation cursor was not followed.');\n  if (items.some(i => i.publishedAt === null)) warnings.push('Some exact publication dates are unavailable; API order is retained.');\n  return { ...profile, status: warnings.some(w => w.includes('rejected')) ? 'partial' : 'ok', items, warnings, error: null };\n}\nfunction rankings(profiles) {\n  const groups = new Map();\n  for (const profile of profiles) for (const item of profile.items) {\n    const key = `${item.platform}:${item.contentType}`;\n    if (!groups.has(key)) groups.set(key, []);\n    if (item.metrics.views !== null && !groups.get(key).some(i => i.id === item.id)) groups.get(key).push(item);\n  }\n  return [...groups].sort(([a], [b]) => a.localeCompare(b)).map(([group, items]) => ({ group, basis: 'known API-reported views within collected sample', top: items.sort((a,b) => b.metrics.views - a.metrics.views || a.id.localeCompare(b.id)).slice(0,3).map(i => ({ id: i.id, views: i.metrics.views, sourceUrl: i.sourceUrl })) }));\n}\nfunction markdown(report) {\n  const esc = value => String(value ?? 'unknown').replace(/[\\\\`*_{}\\[\\]<>|]/g, '\\\\$&').replace(/[\\r\\n]+/g, ' ');\n  const num = value => value === null ? 'unknown' : String(value);\n  const lines = ['# Competitor research', '', `Collected: ${report.startedAt} → ${report.completedAt}`, '', `Schema ${report.schemaVersion}. Up to ${report.limits.limit} items/profile; Instagram Reels only. Sample rankings are not cross-platform comparisons.`, '', `Requests: ${report.usage.requests}/${report.limits.maxRequests}. Modeled credit reservations: ${report.usage.reservedCredits}/${report.limits.maxCredits}. API-reported endpoint credits: ${report.usage.reportedCreditsUsed ?? 'unknown (one or more headers unavailable)'}. Billing debits are not independently verified (monitor/test calls may be waived). Per-request used and remaining-credit headers are recorded in JSON.`, '', 'Missing metrics stay unknown. API-reported zero is retained; upstream defaults can hide missing observations. No AI inference, transcripts, or per-video enrichment.'];\n  for (const p of report.profiles) {\n    lines.push('', `## ${esc(p.url)} — ${p.status}`, '');\n    if (p.error) lines.push(`Failure: ${esc(p.error)}`, '');\n    for (const warning of p.warnings) lines.push(`- ${esc(warning)}`);\n    if (!p.items.length) { lines.push('', p.status === 'ok' ? 'No items returned.' : 'No usable items collected.'); continue; }\n    lines.push('', '| Recent sample (API order) | Type | Published | Views | Likes | Comments |', '|---|---|---|---:|---:|---:|');\n    for (const i of p.items) lines.push(`| [${esc((i.title || i.description || i.id).slice(0,160))}](${i.sourceUrl}) | ${i.contentType} | ${esc(i.publishedAt)} | ${num(i.metrics.views)} | ${num(i.metrics.likes)} | ${num(i.metrics.comments)} |`);\n  }\n  lines.push('', '## Sample top performers', '');\n  for (const group of report.rankings) { lines.push(`### ${group.group}`, '', group.basis, ''); for (const i of group.top) lines.push(`- [${i.id}](${i.sourceUrl}): ${i.views} views`); if (!group.top.length) lines.push('No known view counts available.'); lines.push(''); }\n  lines.push('## Request ledger', '', '| Profile | Attempt | HTTP | Outcome | Reserved credits | Charged header | Remaining header |', '|---|---:|---:|---|---:|---:|---:|');\n  for (const a of report.usage.attempts) lines.push(`| ${esc(a.profileUrl)} | ${a.attempt} | ${a.status ?? 'unknown'} | ${a.outcome} | ${a.reservedCredits} | ${a.creditsUsed ?? 'unknown'} | ${a.creditsRemaining ?? 'unknown'} |`);\n  return `${lines.join('\\n')}\\n`;\n}\n\n// Edit these public settings; store keys only in n8n credentials.\nconst config = { profiles: ['https://www.youtube.com/@NASA', 'https://www.instagram.com/nasa/'], limit: 10, apiBase: 'https://api.socialkit.dev', slackChannel: 'REPLACE_WITH_CHANNEL_ID', destination: 'competitor-monitor-v1:REPLACE_WITH_CHANNEL_ID' };\nconst profiles = normalizeProfiles(config.profiles);\nif (!Number.isInteger(config.limit) || config.limit < 1 || config.limit > 10) throw new Error('limit must be 1–10');\nif (!/^https:\\/\\/[A-Za-z0-9.-]+(?::[0-9]{1,5})?$/.test(config.apiBase)) throw new Error('apiBase must be an HTTPS origin');\nif (!/^[CG][A-Z0-9]+$/.test(config.slackChannel)) throw new Error('Set slackChannel to the Slack channel ID and destination to a stable unique name for that channel.');\nreturn [{ json: { ...config, profiles, startedAt: new Date().toISOString(), owner: String($execution.id) } }];"
      }
    },
    {
      "id": "claim-delivery-lease",
      "name": "Claim delivery lease",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        480,
        0
      ],
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT * FROM sk_monitor_claim($1, $2);",
        "options": {
          "queryReplacement": "={{ [$json.destination, $json.owner] }}"
        }
      }
    },
    {
      "id": "pending-digest-exists",
      "name": "Pending digest exists",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        720,
        0
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 2
          },
          "conditions": [
            {
              "id": "Pending digest exists-condition",
              "leftValue": "={{ Boolean($json.outbox) }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      }
    },
    {
      "id": "build-bounded-requests",
      "name": "Build bounded requests",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        960,
        180
      ],
      "parameters": {
        "jsCode": "const c = $('Configure competitors').first().json; return c.profiles.map(profile => ({json:{...profile,limit:c.limit,apiBase:c.apiBase}}));"
      }
    },
    {
      "id": "collect-socialkit",
      "name": "Collect SocialKit",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.4,
      "position": [
        1200,
        180
      ],
      "parameters": {
        "url": "={{ $json.apiBase + $json.endpoint }}",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendQuery": true,
        "queryParameters": {
          "parameters": [
            {
              "name": "url",
              "value": "={{ $json.url }}"
            },
            {
              "name": "limit",
              "value": "={{ $json.limit }}"
            },
            {
              "name": "full_details",
              "value": "false"
            }
          ]
        },
        "options": {
          "timeout": 60000,
          "batching": {
            "batch": {
              "batchSize": 1,
              "batchInterval": 0
            }
          },
          "redirect": {
            "redirect": {
              "followRedirects": false
            }
          },
          "response": {
            "response": {
              "fullResponse": true,
              "neverError": true,
              "responseFormat": "json"
            }
          }
        }
      },
      "onError": "continueRegularOutput",
      "retryOnFail": false
    },
    {
      "id": "normalize-bounded-report",
      "name": "Normalize bounded report",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1440,
        180
      ],
      "parameters": {
        "jsCode": "/** Pure normalization shared by the CLI and generated n8n artifact. */\nconst VERSION = '1.0.0';\n// Deliberately small HTTP parser: n8n Code sandboxes do not expose the URL global.\n// Reject authority credentials, ports, whitespace and backslashes; caller validates exact paths.\nfunction publicUrl(value) {\n  const m = typeof value === 'string' && /^(https?):\\/\\/([A-Za-z0-9.-]+)(\\/[^?#\\s\\\\]*)?(\\?[^#\\s\\\\]*)?(#[^\\s\\\\]*)?$/i.exec(value);\n  if (!m) throw new Error('Invalid public URL.');\n  const params = (m[4] || '').slice(1).split('&').filter(Boolean).map(p => p.split('='));\n  return { protocol: m[1].toLowerCase() + ':', hostname: m[2].toLowerCase(), pathname: m[3] || '/', search: m[4] || '', hash: m[5] || '', searchParams: { get: key => {const matches=params.filter(p=>p[0]===key);return matches.length===1?matches[0][1]:null;} } };\n}\nfunction parseProfile(value) {\n  let u;\n  try { u = publicUrl(value); } catch { throw new Error('Use a full public Instagram profile or YouTube channel URL.'); }\n  if (!['https:', 'http:'].includes(u.protocol) || u.username || u.password || u.port || u.search || u.hash) throw new Error('Profile URLs must not contain credentials, ports, queries, or fragments.');\n  const path = u.pathname.replace(/\\/+$/, '');\n  if (['youtube.com', 'www.youtube.com', 'm.youtube.com'].includes(u.hostname) && /^\\/(?:@[\\p{L}\\p{N}_.-]+|(?:channel|c|user)\\/[A-Za-z0-9_.-]+)$/u.test(path)) {\n    return { platform: 'youtube', url: `https://www.youtube.com${path}`, endpoint: '/youtube/videos' };\n  }\n  if (['instagram.com', 'www.instagram.com', 'm.instagram.com'].includes(u.hostname) && /^\\/[A-Za-z0-9_.]{1,30}$/.test(path) && !['p', 'reel', 'reels', 'tv', 'explore', 'accounts', 'stories'].includes(path.slice(1).toLowerCase())) {\n    return { platform: 'instagram', url: `https://www.instagram.com${path.toLowerCase()}/`, endpoint: '/instagram/channel-reels' };\n  }\n  throw new Error('Expected a YouTube channel URL or Instagram profile URL, not a post, video, or playlist.');\n}\nfunction normalizeProfiles(values) {\n  if (!Array.isArray(values) || values.length < 1 || values.length > 3) throw new Error('Configure one to three profiles.');\n  const profiles = values.map(parseProfile);\n  if (new Set(profiles.map(p => p.url)).size !== profiles.length) throw new Error('Profiles must be distinct.');\n  return profiles;\n}\nfunction listCredits(platform, limit) { return Math.max(1, Math.ceil(limit / (platform === 'instagram' ? 20 : 50))); }\nfunction metric(value) { return typeof value === 'number' && Number.isFinite(value) && value >= 0 ? value : null; }\nfunction instant(value) {\n  if (typeof value !== 'string' || !/^\\d{4}-\\d{2}-\\d{2}T/.test(value)) return null;\n  const t = Date.parse(value); return Number.isFinite(t) ? new Date(t).toISOString() : null;\n}\nfunction textValue(value) { return typeof value === 'string' && value.trim() ? value.trim() : null; }\nfunction normalizeItem(item, profile, fetchedAt) {\n  if (!item || typeof item !== 'object') throw new Error('Invalid item object.');\n  let u; try { u = publicUrl(item.url); } catch { throw new Error('Missing or invalid source URL.'); }\n  if (!['http:', 'https:'].includes(u.protocol) || u.username || u.password || u.port) throw new Error('Unsafe source URL.');\n  let id, sourceUrl, contentType;\n  if (profile.platform === 'youtube') {\n    if (!['youtube.com', 'www.youtube.com', 'm.youtube.com', 'youtu.be'].includes(u.hostname)) throw new Error('Wrong source host.');\n    const urlId = u.hostname === 'youtu.be' ? u.pathname.slice(1) : u.pathname === '/watch' ? u.searchParams.get('v') : /^\\/shorts\\/([\\w-]{11})\\/?$/.exec(u.pathname)?.[1];\n    id = item.videoId;\n    if (!/^[\\w-]{11}$/.test(id || '') || id !== urlId) throw new Error('Video ID does not match source URL.');\n    contentType = u.pathname.startsWith('/shorts/') || item.contentType === 'short' ? 'short' : 'video';\n    sourceUrl = `https://www.youtube.com/watch?v=${id}`;\n  } else {\n    if (!['instagram.com', 'www.instagram.com', 'm.instagram.com'].includes(u.hostname)) throw new Error('Wrong source host.');\n    const urlId = /^\\/(?:p|reel|reels)\\/([\\w-]+)\\/?$/.exec(u.pathname)?.[1];\n    id = item.shortcode;\n    if (!/^[\\w-]{1,64}$/.test(id || '') || id !== urlId) throw new Error('Reel shortcode does not match source URL.');\n    if (item.type !== 'reel' && item.productType !== 'clips') throw new Error('Non-Reel item in Reels feed.');\n    const author = item.author?.username;\n    contentType = 'reel'; sourceUrl = `https://www.instagram.com/reel/${id}/`;\n  }\n  let publishedAt = instant(item.publishedAt);\n  if (profile.platform === 'instagram' && typeof item.timestamp === 'number' && item.timestamp > 0 && item.timestamp < 8640000000000) publishedAt = new Date(item.timestamp * 1000).toISOString();\n  return { id: `${profile.platform}:${id}`, platform: profile.platform, contentType, profileUrl: profile.url, sourceUrl,\n    authorUsername: profile.platform === 'instagram' ? textValue(item.author?.username) : null,\n    title: textValue(item.title), description: textValue(profile.platform === 'instagram' ? item.caption : item.description),\n    publishedAt, fetchedAt, metrics: { views: metric(item.views), likes: metric(item.likes), comments: metric(item.comments) } };\n}\nfunction normalizeResponse(body, profile, limit, fetchedAt) {\n  if (body?.success !== true || !body.data) throw new Error('API did not return a successful data object.');\n  const data = body.data;\n  const identity = profile.platform === 'instagram' ? data.profileUrl : data.url;\n  if (!identity && !(profile.platform === 'instagram' && typeof data.username === 'string' && data.username)) throw new Error('Response is missing a verifiable profile identity.');\n  if (identity && parseProfile(identity).url !== profile.url) throw new Error('Response profile identity does not match request.');\n  if (profile.platform === 'instagram' && data.username && data.username.toLowerCase() !== publicUrl(profile.url).pathname.split('/')[1]) throw new Error('Response username does not match request.');\n  const source = profile.platform === 'youtube' ? data.results : data.items;\n  if (!Array.isArray(source)) throw new Error('Response is missing the expected item array.');\n  if (source.length > limit) throw new Error('API exceeded requested item limit; stop further requests.');\n  const warnings = [], items = [], seen = new Set();\n  for (const [index, raw] of source.entries()) {\n    try { const item = normalizeItem(raw, profile, fetchedAt); if (!seen.has(item.id)) { items.push(item); seen.add(item.id); } else warnings.push(`Duplicate item ${index + 1} omitted.`); }\n    catch (error) { warnings.push(`Item ${index + 1} rejected: ${error.message}`); }\n  }\n  if (source.length && !items.length) throw new Error(`All ${source.length} items failed identity/schema validation.`);\n  for (const item of items) if (item.authorUsername && item.authorUsername.toLowerCase() !== publicUrl(profile.url).pathname.split('/')[1]) warnings.push(`${item.id} has primary author @${item.authorUsername}; feed membership does not prove original authorship (shared/collaborative posts may differ).`);\n  if (data.hasMore) warnings.push('More items exist; continuation cursor was not followed.');\n  if (items.some(i => i.publishedAt === null)) warnings.push('Some exact publication dates are unavailable; API order is retained.');\n  return { ...profile, status: warnings.some(w => w.includes('rejected')) ? 'partial' : 'ok', items, warnings, error: null };\n}\nfunction rankings(profiles) {\n  const groups = new Map();\n  for (const profile of profiles) for (const item of profile.items) {\n    const key = `${item.platform}:${item.contentType}`;\n    if (!groups.has(key)) groups.set(key, []);\n    if (item.metrics.views !== null && !groups.get(key).some(i => i.id === item.id)) groups.get(key).push(item);\n  }\n  return [...groups].sort(([a], [b]) => a.localeCompare(b)).map(([group, items]) => ({ group, basis: 'known API-reported views within collected sample', top: items.sort((a,b) => b.metrics.views - a.metrics.views || a.id.localeCompare(b.id)).slice(0,3).map(i => ({ id: i.id, views: i.metrics.views, sourceUrl: i.sourceUrl })) }));\n}\nfunction markdown(report) {\n  const esc = value => String(value ?? 'unknown').replace(/[\\\\`*_{}\\[\\]<>|]/g, '\\\\$&').replace(/[\\r\\n]+/g, ' ');\n  const num = value => value === null ? 'unknown' : String(value);\n  const lines = ['# Competitor research', '', `Collected: ${report.startedAt} → ${report.completedAt}`, '', `Schema ${report.schemaVersion}. Up to ${report.limits.limit} items/profile; Instagram Reels only. Sample rankings are not cross-platform comparisons.`, '', `Requests: ${report.usage.requests}/${report.limits.maxRequests}. Modeled credit reservations: ${report.usage.reservedCredits}/${report.limits.maxCredits}. API-reported endpoint credits: ${report.usage.reportedCreditsUsed ?? 'unknown (one or more headers unavailable)'}. Billing debits are not independently verified (monitor/test calls may be waived). Per-request used and remaining-credit headers are recorded in JSON.`, '', 'Missing metrics stay unknown. API-reported zero is retained; upstream defaults can hide missing observations. No AI inference, transcripts, or per-video enrichment.'];\n  for (const p of report.profiles) {\n    lines.push('', `## ${esc(p.url)} — ${p.status}`, '');\n    if (p.error) lines.push(`Failure: ${esc(p.error)}`, '');\n    for (const warning of p.warnings) lines.push(`- ${esc(warning)}`);\n    if (!p.items.length) { lines.push('', p.status === 'ok' ? 'No items returned.' : 'No usable items collected.'); continue; }\n    lines.push('', '| Recent sample (API order) | Type | Published | Views | Likes | Comments |', '|---|---|---|---:|---:|---:|');\n    for (const i of p.items) lines.push(`| [${esc((i.title || i.description || i.id).slice(0,160))}](${i.sourceUrl}) | ${i.contentType} | ${esc(i.publishedAt)} | ${num(i.metrics.views)} | ${num(i.metrics.likes)} | ${num(i.metrics.comments)} |`);\n  }\n  lines.push('', '## Sample top performers', '');\n  for (const group of report.rankings) { lines.push(`### ${group.group}`, '', group.basis, ''); for (const i of group.top) lines.push(`- [${i.id}](${i.sourceUrl}): ${i.views} views`); if (!group.top.length) lines.push('No known view counts available.'); lines.push(''); }\n  lines.push('## Request ledger', '', '| Profile | Attempt | HTTP | Outcome | Reserved credits | Charged header | Remaining header |', '|---|---:|---:|---|---:|---:|---:|');\n  for (const a of report.usage.attempts) lines.push(`| ${esc(a.profileUrl)} | ${a.attempt} | ${a.status ?? 'unknown'} | ${a.outcome} | ${a.reservedCredits} | ${a.creditsUsed ?? 'unknown'} | ${a.creditsRemaining ?? 'unknown'} |`);\n  return `${lines.join('\\n')}\\n`;\n}\n\nconst c = $('Configure competitors').first().json, fetchedAt = new Date().toISOString();\nconst responses = $input.all();\nif (responses.length !== c.profiles.length) throw new Error('Unexpected response count');\nconst attempts = [], profiles = responses.map((row,index) => {\n const p=c.profiles[index], r=row.json, status=r.statusCode ?? null;\n const remaining=r.headers?.['x-credits-remaining'],used=r.headers?.['x-credits-used'];\n attempts.push({profileUrl:p.url,endpoint:p.endpoint,attempt:1,status,reservedCredits:1,creditsUsed:/^\\d+$/.test(String(used))?Number(used):null,creditsRemaining:/^\\d+$/.test(String(remaining))?Number(remaining):null});\n try { if(status!==200) throw new Error(status===401||status===403?'Authentication rejected; check SocialKit credentials.':status===402?'Insufficient credits.':'Collection failed (HTTP '+(status ?? 'unknown')+'); retry next run.'); return normalizeResponse(r.body,p,c.limit,fetchedAt); }\n catch(error) {return {...p,status:'failed',items:[],warnings:[],error:status===200?error.message:(status===401||status===403?'Authentication rejected; check SocialKit credentials.':status===402?'Insufficient credits.':'Collection failed (HTTP '+(status ?? 'unknown')+'); retry next run.')};}\n});\nif(profiles.every(p=>p.status==='failed')) throw new Error('All profiles failed; no digest or delivered marks created. Check Collect SocialKit status codes and credentials.');\nconst report={schemaVersion:VERSION,startedAt:c.startedAt,completedAt:fetchedAt,limits:{limit:c.limit,maxRequests:3,maxCredits:3,retries:0},profiles,rankings:rankings(profiles),usage:{requests:responses.length,reservedCredits:responses.length,chargedCredits:null,reportedCreditsUsed:attempts.every(a=>a.creditsUsed!==null)?attempts.reduce((sum,a)=>sum+a.creditsUsed,0):null,attempts}};\nreturn [{json:{...c,report}}];"
      }
    },
    {
      "id": "stage-persistent-outbox",
      "name": "Stage persistent outbox",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        1680,
        180
      ],
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT * FROM sk_monitor_stage($1, $2, $3::jsonb);",
        "options": {
          "queryReplacement": "={{ [$json.destination, $json.owner, JSON.stringify($json.report)] }}"
        }
      }
    },
    {
      "id": "has-new-digest",
      "name": "Has new digest",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        1920,
        180
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 2
          },
          "conditions": [
            {
              "id": "Has new digest-condition",
              "leftValue": "={{ Boolean($json.outbox) }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      }
    },
    {
      "id": "guard-delivery-lease",
      "name": "Guard delivery lease",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        2160,
        0
      ],
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT * FROM sk_monitor_guard($1, $2);",
        "options": {
          "queryReplacement": "={{ [$('Configure competitors').first().json.destination, $('Configure competitors').first().json.owner] }}"
        }
      }
    },
    {
      "id": "format-slack-digest",
      "name": "Format Slack digest",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2400,
        0
      ],
      "parameters": {
        "jsCode": "const state=$input.first().json, outbox=state.outbox;\nif(!outbox?.items?.length) return [];\nconst esc=s=>String(s??'unknown').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/[\\r\\n]+/g,' ');\nconst num=n=>n===null?'unknown':n;\nconst lines=['*SocialKit competitor monitor* — digest '+esc(outbox.digestId),'Collected '+outbox.collectedAt+'; '+outbox.items.length+' new sampled items.'];\nfor(const i of outbox.items) lines.push('• '+i.platform+' / '+i.contentType+' · '+esc(i.profileUrl)+(i.authorUsername?' · primary author @'+esc(i.authorUsername):'')+'\\n<'+i.sourceUrl+'|'+esc((i.title||i.description||i.id).slice(0,140)).replace(/\\|/g,' ') +'> · views '+num(i.metrics.views)+' · likes '+num(i.metrics.likes)+' · comments '+num(i.metrics.comments));\nfor(const p of outbox.profiles) if(p.status!=='ok') lines.push('Partial collection: '+esc(p.url)+' — '+esc(p.error||p.warnings.join('; ')));\nlines.push('Metrics are platform-specific; unknown ≠ zero. No AI inference.');\nreturn [{json:{...state,digestId:outbox.digestId,text:lines.join('\\n'),channel:$('Configure competitors').first().json.slackChannel}}];"
      }
    },
    {
      "id": "send-slack-digest",
      "name": "Send Slack digest",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.4,
      "position": [
        2640,
        0
      ],
      "parameters": {
        "method": "POST",
        "url": "https://slack.com/api/chat.postMessage",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ { channel: $json.channel, text: $json.text, unfurl_links: false, unfurl_media: false } }}",
        "options": {
          "timeout": 30000,
          "redirect": {
            "redirect": {
              "followRedirects": false
            }
          },
          "response": {
            "response": {
              "fullResponse": true,
              "neverError": true,
              "responseFormat": "json"
            }
          }
        }
      },
      "retryOnFail": false
    },
    {
      "id": "confirm-slack-delivery",
      "name": "Confirm Slack delivery",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2880,
        0
      ],
      "parameters": {
        "jsCode": "const r=$input.first().json; if(r.statusCode!==200||r.body?.ok!==true||typeof r.body.ts!=='string'||!r.body.ts) throw new Error('Slack did not confirm delivery. Pending digest retained; wait for lease expiration before retrying. An ambiguous timeout can duplicate a message.'); const sent=$('Format Slack digest').first().json; if(r.body.channel!==sent.channel) throw new Error('Slack channel confirmation mismatch; pending digest retained.'); return [{json:{destination:sent.destination,owner:$('Configure competitors').first().json.owner,digestId:sent.digestId,slackTs:r.body.ts}}];"
      }
    },
    {
      "id": "commit-confirmed-delivery",
      "name": "Commit confirmed delivery",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        3120,
        0
      ],
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT sk_monitor_ack($1, $2, $3, $4) AS delivered_count;",
        "options": {
          "queryReplacement": "={{ [$json.destination, $json.owner, $json.digestId, $json.slackTs] }}"
        }
      }
    }
  ],
  "connections": {
    "Manual run": {
      "main": [
        [
          {
            "node": "Configure competitors",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Daily schedule": {
      "main": [
        [
          {
            "node": "Configure competitors",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Configure competitors": {
      "main": [
        [
          {
            "node": "Claim delivery lease",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Claim delivery lease": {
      "main": [
        [
          {
            "node": "Pending digest exists",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Pending digest exists": {
      "main": [
        [
          {
            "node": "Guard delivery lease",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Build bounded requests",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build bounded requests": {
      "main": [
        [
          {
            "node": "Collect SocialKit",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Collect SocialKit": {
      "main": [
        [
          {
            "node": "Normalize bounded report",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalize bounded report": {
      "main": [
        [
          {
            "node": "Stage persistent outbox",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Stage persistent outbox": {
      "main": [
        [
          {
            "node": "Has new digest",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Has new digest": {
      "main": [
        [
          {
            "node": "Guard delivery lease",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Guard delivery lease": {
      "main": [
        [
          {
            "node": "Format Slack digest",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Format Slack digest": {
      "main": [
        [
          {
            "node": "Send Slack digest",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Send Slack digest": {
      "main": [
        [
          {
            "node": "Confirm Slack delivery",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Confirm Slack delivery": {
      "main": [
        [
          {
            "node": "Commit confirmed delivery",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1",
    "executionTimeout": 600,
    "saveDataErrorExecution": "all",
    "saveDataSuccessExecution": "none",
    "saveManualExecutions": false,
    "timezone": "UTC"
  },
  "pinData": {},
  "tags": []
}
