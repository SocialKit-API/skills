# Benchmark readiness — 2026-09-21

The owner explicitly allowed skipping third-party work on 2026-09-21. Authenticated ScrapeCreators/Apify calls, the three-provider pilot, final comparison measurement and publication of comparative findings are **skipped under that instruction**, not completed measurements. No fabricated performance or usability values are supplied.

| Gate | Current evidence / action |
| --- | --- |
| Protocol before measurement | Written in `METHODOLOGY.md` before any benchmark calls; views optional consistently, English caption-only scope, exact single-Reel lookup. |
| Useful existing mechanics reviewed | SocialKit API monitor runner/truth set reviewed; deadlines, fresh requests and polling adapted. No operational/customer-derived inputs imported. |
| Exact Apify Actor/build | `provider-pins.json` contains read-only official API snapshots, IDs/builds, status and public pricing. No Actor was executed. |
| Adapter/local verification | `node --test examples/benchmark/benchmark.test.mjs`; synthetic fixtures and no spend. |
| Credentials and balances | Three-provider credentials unavailable in investigated deployment environments; user excluded this third-party work. Do not create accounts/purchase access. |
| Charge/cohort evidence | Requires an actual SocialKit pilot plus account pricing/cohort review. Current API reports `X-Credits-Used`; missing pricing-version header remains null. ScrapeCreators charge field is `credits_charged`; Apify is terminal `usageTotalUsd`. |
| Budget | Example protocol cap: $5 across pilot/full runs; a future operator must confirm access and remaining budget before executing. No purchase or paid run was performed. Example bounded pilot maximum $0.36; 20+3 inputs/workload full maximum $4.14. Future runs subtract all prior reservations, including unknown failures. |
| Public input selection | Four explicitly exploratory documentation examples supplied for pilot. Full 20–30 diverse public inputs/workload and prechecks are not fabricated. Full sample awaits resumption of comparative measurement. |
| Manual source QA | Required because SocialKit caption response echoes requested URL and omits language/source. No independent identity/source assurance claimed. |
| Full lock | Blocked by incomplete/excluded three-provider pilot; no fake lock is provided. Runner has a hash-bound lock gate when resumed. |
| Complete run/analysis/QA | Runnable code provided; actual comparative evidence intentionally absent. Retain every attempted failure and every planned not-run result in future runs. |
| Article publication | No new speed/reliability comparison without the above evidence. Link runner/methodology as reproducibility assets and state results are pending. |

A future resumption must explicitly restore the excluded third-party scope, confirm provider access and remaining cap, run the pilot, document normalization/charge/source QA, freeze full inputs and lock methodology, run the complete sample, manually QA, and publish results with the existing article together. Provider comparison claims must not be inferred from the development-only SocialKit smoke test.
